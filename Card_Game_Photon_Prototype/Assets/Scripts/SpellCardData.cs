public class SpellCardData : CardData {
	public SpellCardData(int pId,
                         string pName,
                         string pDescription,
                         int pRegCost,
                         int pTurboCost) : base(pId, pName, pDescription, pRegCost, pTurboCost)
    {
        // Empty
    }
}
